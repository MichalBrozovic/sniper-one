/******/ (() => { // webpackBootstrap
/******/ 	"use strict";
function _typeof(o) { "@babel/helpers - typeof"; return _typeof = "function" == typeof Symbol && "symbol" == typeof Symbol.iterator ? function (o) { return typeof o; } : function (o) { return o && "function" == typeof Symbol && o.constructor === Symbol && o !== Symbol.prototype ? "symbol" : typeof o; }, _typeof(o); }
function ownKeys(e, r) { var t = Object.keys(e); if (Object.getOwnPropertySymbols) { var o = Object.getOwnPropertySymbols(e); r && (o = o.filter(function (r) { return Object.getOwnPropertyDescriptor(e, r).enumerable; })), t.push.apply(t, o); } return t; }
function _objectSpread(e) { for (var r = 1; r < arguments.length; r++) { var t = null != arguments[r] ? arguments[r] : {}; r % 2 ? ownKeys(Object(t), !0).forEach(function (r) { _defineProperty(e, r, t[r]); }) : Object.getOwnPropertyDescriptors ? Object.defineProperties(e, Object.getOwnPropertyDescriptors(t)) : ownKeys(Object(t)).forEach(function (r) { Object.defineProperty(e, r, Object.getOwnPropertyDescriptor(t, r)); }); } return e; }
function _defineProperty(e, r, t) { return (r = _toPropertyKey(r)) in e ? Object.defineProperty(e, r, { value: t, enumerable: !0, configurable: !0, writable: !0 }) : e[r] = t, e; }
function _toPropertyKey(t) { var i = _toPrimitive(t, "string"); return "symbol" == _typeof(i) ? i : i + ""; }
function _toPrimitive(t, r) { if ("object" != _typeof(t) || !t) return t; var e = t[Symbol.toPrimitive]; if (void 0 !== e) { var i = e.call(t, r || "default"); if ("object" != _typeof(i)) return i; throw new TypeError("@@toPrimitive must return a primitive value."); } return ("string" === r ? String : Number)(t); }
/**
 * Swiperize 4.2 - Lightweight Sniper Edition
 * Osekáno o fullscreen a scrollbar width. Zaměřeno na výkon a .swiper-helper strukturu.
 */
window.swiperize = function (_ref) {
  var containers = _ref.containers,
    slide = _ref.slide,
    _ref$customOptions = _ref.customOptions,
    customOptions = _ref$customOptions === void 0 ? {} : _ref$customOptions,
    _ref$swiperOptions = _ref.swiperOptions,
    swiperOptions = _ref$swiperOptions === void 0 ? {} : _ref$swiperOptions;
  var containerArray = Array.isArray(containers) ? containers : [containers];
  var slideArray = Array.isArray(slide) ? slide : [slide];
  var fallbackTimeout = 4000;
  window.sniperSwipers = window.sniperSwipers || {};
  var swiperizedCount = 0;
  var initializeSwiper = function initializeSwiper(selector, slideClass) {
    var originalContainer = document.querySelector(selector);
    if (!originalContainer || originalContainer.classList.contains("swiperized")) return;
    if (window.sniperSwipers[selector]) {
      window.sniperSwipers[selector].destroy(true, true);
    }
    var safeClass = selector.replace(/[#.]/g, "") // Odstraní tečky a mřížky
    .replace(/[^a-zA-Z0-9]/g, "-") // Všechno co není písmeno nebo číslo nahradí pomlčkou
    .replace(/-+/g, "-");

    // 1. STRUKTURA: Wrapper -> Helper (šipky) -> Root (overflow)
    var containerWrapper = document.createElement("div");
    containerWrapper.className = "swiper-container-wrapper swiper-container-wrapper-".concat(safeClass);
    var swiperHelper = document.createElement("div");
    swiperHelper.className = "swiper-helper";
    var swiperRoot = document.createElement("div");
    swiperRoot.className = "swiper swiper-".concat(safeClass);
    swiperRoot.style.width = "100%";
    swiperRoot.style.overflow = "hidden";

    // 2. TRANSFORMACE PŮVODNÍHO ELEMENTU
    var wrapper = originalContainer;
    wrapper.classList.add("swiper-wrapper", "swiperized");
    wrapper.style.display = "flex";
    wrapper.style.opacity = "0";

    // 3. PŘÍPRAVA SLIDŮ (včetně data-src)
    var slides = wrapper.querySelectorAll(slideClass);
    slides.forEach(function (s) {
      s.classList.add("swiper-slide");
      var img = s.querySelector(".image img") || s.querySelector("img");
      if (img !== null && img !== void 0 && img.getAttribute("data-src")) {
        img.setAttribute("src", img.getAttribute("data-src"));
      }
    });

    // 4. SLOŽENÍ DOM
    wrapper.before(containerWrapper);
    containerWrapper.appendChild(swiperHelper);
    swiperHelper.appendChild(swiperRoot);
    swiperRoot.appendChild(wrapper);
    var currentSwiperOptions = _objectSpread({}, swiperOptions);

    // PAGINATION
    if (customOptions.dots) {
      var pag = document.createElement("div");
      var pagClass = "swiper-pagination-".concat(safeClass);
      pag.className = "swiper-pagination ".concat(pagClass);
      containerWrapper.appendChild(pag);
      currentSwiperOptions.pagination = {
        el: ".".concat(pagClass),
        clickable: true
      };
    }

    // ARROWS (do helperu)
    if (customOptions.arrows) {
      var prev = document.createElement("div");
      var next = document.createElement("div");
      var pClass = "swiper-button-prev-".concat(safeClass);
      var nClass = "swiper-button-next-".concat(safeClass);
      prev.className = "swiper-button-prev ".concat(pClass);
      next.className = "swiper-button-next ".concat(nClass);
      swiperHelper.append(prev, next);
      currentSwiperOptions.navigation = {
        nextEl: ".".concat(nClass),
        prevEl: ".".concat(pClass)
      };
    }
    if (customOptions.infinity) currentSwiperOptions.loop = true;

    // 5. START
    var swiperInstance = new Swiper(swiperRoot, currentSwiperOptions);
    window.sniperSwipers[selector] = swiperInstance;
    setTimeout(function () {
      wrapper.style.opacity = "1";
    }, 50);

    // Schování šipek při malém počtu produktů
    var total = wrapper.querySelectorAll(".swiper-slide").length;
    var visible = swiperInstance.params.slidesPerView === "auto" ? swiperInstance.slidesPerViewDynamic() : swiperInstance.params.slidesPerView;
    if (total <= visible) {
      containerWrapper.querySelectorAll(".swiper-button-prev, .swiper-button-next").forEach(function (b) {
        return b.style.display = 'none';
      });
    }
  };

  // 6. OBSERVER
  var swiperObserver = new IntersectionObserver(function (entries, observer) {
    entries.forEach(function (entry) {
      if (entry.isIntersecting) {
        var sel = entry.target.getAttribute("data-selector");
        var index = containerArray.indexOf(sel);
        var sClass = slideArray[Math.min(index, slideArray.length - 1)];
        initializeSwiper(sel, sClass);
        observer.unobserve(entry.target);
        swiperizedCount++;
        if (swiperizedCount === containerArray.length) observer.disconnect();
      }
    });
  }, {
    rootMargin: "0px 0px 400px 0px",
    threshold: 0.1
  });
  var run = function run() {
    containerArray.forEach(function (selector) {
      var el = document.querySelector(selector);
      if (el && !el.classList.contains("swiperized")) {
        el.setAttribute("data-selector", selector);
        swiperObserver.observe(el);
        setTimeout(function () {
          if (!el.classList.contains("swiperized")) {
            var index = containerArray.indexOf(selector);
            initializeSwiper(selector, slideArray[Math.min(index, slideArray.length - 1)]);
          }
        }, fallbackTimeout);
      }
    });
  };
  run();
  document.addEventListener("shoptet.content.updated", run);
};
window.swiperize.setLastVisibleSlide = function (swiper) {
  swiper.slides.forEach(function (s) {
    return s.classList.remove("swiper-slide-last-visible");
  });
  var perView = swiper.params.slidesPerView === "auto" ? swiper.slidesPerViewDynamic() : Math.round(swiper.params.slidesPerView);
  var lastIndex = (swiper.activeIndex + perView) % swiper.slides.length;
  for (var i = lastIndex; i < swiper.slides.length; i++) {
    if (lastIndex === 0 && i === 0) break;
    swiper.slides[i].classList.add("swiper-slide-last-visible");
  }
};
/******/ })()
;